package com.michaelflisar.dialogs.classes

import com.michaelflisar.dialogs.frequency.R

enum class RepeatType(val typeRes: Int) {
    Regular(R.string.dialogs_repeat_type_regular),
    Irregular(R.string.dialogs_repeat_type_irregular)
}