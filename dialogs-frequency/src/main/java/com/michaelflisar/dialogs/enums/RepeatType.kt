package com.michaelflisar.dialogs.enums

import com.michaelflisar.dialogs.frequency.R

enum class RepeatType(val typeRes: Int) {
    Regular(R.string.mdf_repeat_type_regular),
    Irregular(R.string.mdf_repeat_type_irregular)
}