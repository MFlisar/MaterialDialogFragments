package com.michaelflisar.dialogs.classes

object Constants {

    // must be the same index as the index in MaterialDialogs
    const val INDEX_POSITIVE = 0
    const val INDEX_NEGATIVE = 1
    const val INDEX_NEUTRAL = 2
}